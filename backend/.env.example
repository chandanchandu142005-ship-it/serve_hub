# ==================================================================
# Servehub Backend Environment Configuration
# ==================================================================

# Database connection string (MongoDB or MongoDB Atlas)
MONGO_URI=mongodb://127.0.0.1:27017/servehub
MONGODB_URI=mongodb://127.0.0.1:27017/servehub

# JWT Secret key for signing authentication tokens
JWT_SECRET=servehub_super_secret_jwt_key_2026

# Server Port & Client URL
PORT=4000
CLIENT_URL=http://localhost:5501

# ------------------------------------------------------------------
# 1. REAL EMAIL OTP CONFIGURATION (Gmail SMTP)
# ------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=chandanchandu142005@gmail.com
SMTP_PASS=rfjs jiii gpty bsrg
MAIL_FROM=ServeHub <no-reply@servehub.in>

# ------------------------------------------------------------------
# 2. REAL MOBILE SMS OTP CONFIGURATION (Twilio)
# ------------------------------------------------------------------
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_FROM=your_twilio_phone_number

# ------------------------------------------------------------------
# 3. REAL WHATSAPP OTP CONFIGURATION (Meta Cloud API / Twilio)
# ------------------------------------------------------------------
WHATSAPP_TOKEN=your_meta_whatsapp_token
WHATSAPP_PHONE_ID=your_whatsapp_phone_number_id
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# ------------------------------------------------------------------
# OTP DEMO DISPLAY SWITCH
# ------------------------------------------------------------------
OTP_BYPASS=true

# ------------------------------------------------------------------
# 4. GOOGLE OAUTH 2.0 CONFIGURATION
# Obtain Client ID from Google Cloud Console > Credentials > OAuth 2.0 Client IDs
# ------------------------------------------------------------------
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
ALLOW_MOCK_GOOGLE=false
