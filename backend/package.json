{
  "name": "servehub-backend",
  "version": "1.0.0",
  "description": "Servehub marketplace API — Express + MongoDB (with offline JSON-store fallback)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:web": "node --watch server.js",
    "dev:api": "node --watch server.js",
    "check": "node --check server.js",
    "mail:test": "node scripts/test-mail.js",
    "notify:test": "node scripts/test-notify.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.8.0",
    "mongoose": "^9.9.1",
    "nodemailer": "^9.0.5"
  }
}
